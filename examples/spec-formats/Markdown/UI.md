## UI – Checkout Screen

Component: CheckoutPage

Fields:
  - Items list (editable)
  - Customer type selector
  - Total price (readonly)

Rules:
  Total must update on item change
  Submit disabled if no items
